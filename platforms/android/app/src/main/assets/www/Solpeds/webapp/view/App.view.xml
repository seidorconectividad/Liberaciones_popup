<mvc:View
		controllerName="com.prodac.libsolped.masterdetail.controller.App"
		xmlns:mvc="sap.ui.core.mvc"
		displayBlock="true"
		busy="{appView>/busy}"
		busyIndicatorDelay="{appView>/delay}"
		xmlns="sap.m">
	<SplitApp id="idAppControl" mode="HideMode"/>
</mvc:View>